[package]
name = "treebolt"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
csv = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlparser = { version = "=0.60.0", features = ["serde", "visitor"] }